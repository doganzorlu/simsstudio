# Domain & Application Architecture

This document defines the core domain models, application use cases, and module capabilities for SIMSStudio.

## Modules

### 1. SimsConverter.Domain
Contains core domain models, enums, business entities, and validation contracts that represent conversion concepts across Sims game versions.

#### Key Entities & Types
- **GameVersion**: Enum representing supported Sims game versions (`Sims3`, `Sims4`, `Unknown`).
- **PackageContainerKind**: Enum representing recognized package container formats (`Unknown`, `Dbpf`, `Sims3Pack`).
- **PackageDetectionConfidence**: Enum representing confidence level of detection (`None`, `Low`, `High`).
- **PackageCompressionKind**: Enum representing entry compression algorithm (`Unknown`, `None`, `Zlib`, `RefPack`).
- **Sims3PackPayloadKind**: Enum representing scanned payload candidate type (`Unknown`, `DbpfPackage`, `PngPreview`).
- **TextureClassificationKind**: Enum representing texture resource classification (`Unknown`, `TextureCandidate`, `KnownTexture`, `UnsupportedTexture`).
- **TextureMapKind**: Enum representing texture map role (`Unknown`, `Diffuse`, `Normal`, `Specular`, `Alpha`, `Mask`, `Icon`, `Thumbnail`).
- **DdsTextureFormatKind**: Enum representing DDS format family (`Unknown`, `Dxt1`, `Dxt3`, `Dxt5`, `Ati1`, `Ati2`, `UncompressedRgba`).
- **MeshClassificationKind**: Enum representing mesh resource classification (`Unknown`, `MeshCandidate`, `KnownMesh`, `UnsupportedMesh`).
- **MeshRoleKind**: Enum representing mesh role/taxonomy (`Unknown`, `Geometry`, `Rig`, `Skeleton`, `Morph`, `Slot`).
- **CanonicalCoordinateSystem**: Enum representing 3D coordinate system conventions (`Unknown`, `RightHandedYUp`, `RightHandedZUp`, `LeftHandedYUp`, `LeftHandedZUp`).
- **MeshVector2**, **MeshVector3**, **MeshVector4**: Pure C# value structs for 2D, 3D, and 4D float vectors.
- **CanonicalBoneWeight**: Struct representing bone index and weight binding (`BoneIndex`, `Weight`).
- **CanonicalVertex**: Record representing 3D vertex data (`Position`, `Normal?`, `Tangent?`, `Uv0?`, `Uv1?`, `BoneWeights?`). Enforces get-only properties and defensive copy snapshot immutability on `BoneWeights`.
- **CanonicalFace**: Struct representing triangle vertex indices (`A`, `B`, `C`).
- **CanonicalMaterialSlot**: Record representing material slot binding (`SlotIndex`, `MaterialName`).
- **CanonicalMesh**: Immutable aggregate record representing a game-agnostic 3D mesh (`Name`, `Vertices`, `Faces`, `Materials`, `CoordinateSystem`, `SourceGameVersion`, `Issues`). Enforces get-only properties and defensive copy snapshot immutability on all collection properties (`Vertices`, `Faces`, `Materials`, `Issues`), preventing init-setter or `with` expression bypasses.
- **CanonicalMeshValidationResult**: Value record specifying mesh validation outcome (`IsSuccess`, `Issues`).
- **ICanonicalMeshValidator**: Primary contract for canonical mesh validation (`Validate`).
- **CanonicalMeshValidator**: Safe implementation validating vertex positions (`MESHV005`), face index bounds (`MESHV003`), degenerate triangles (`MESHV004`), bone weight sums (`MESHV006`), and non-empty vertex/face lists (`MESHV001`, `MESHV002`).
- **PackageResourceId**: Value object record representing resource identification tuple (`TypeId` uint, `GroupId` uint, `InstanceId` ulong, with formatted key representation).
- **ConversionIssueSeverity**: Enum representing diagnostic severity (`Info`, `Warning`, `Error`, `Fatal`).
- **ConversionIssue**: Immutable record tracking a specific issue encountered during analysis/conversion.
- **ConversionReport**: Aggregate tracking all issues, source/target versions, execution timestamp, and overall error status (`HasErrors`).
- **PackageDetectionResult**: Immutable record containing detection outcomes (`ContainerKind`, `DetectedGameVersion`, `Confidence`, versioning, issues).
- **DbpfHeader**: Record containing parsed DBPF header layout metadata (`Magic`, `MajorVersion`, `MinorVersion`, `IndexEntryCount`, `IndexOffset`, `IndexSizeBytes`).
- **PackageResourceEntry**: Immutable record representing a parsed resource entry (`PackageResourceId Id`, `DataOffset`, `CompressedSize`, `DecompressedSize`, `PackageCompressionKind`, `CompressionFlags`).
- **DbpfParseResult**: Immutable record containing parse operation status (`IsSuccess`, `DbpfHeader?`, `Entries`, `Issues`).
- **PackageResourceExportRequest**: Value record specifying source package path, offset, size, target output path, and overwrite policy (`AllowOverwrite`).
- **PackageResourceExportResult**: Value record specifying export operation status (`IsSuccess`), source path, output path, exported byte count, and issues.
- **Sims3PackXmlMetadata**: Record containing parsed Sims3Pack XML manifest fields (`RootElementName`, `DeclaredEncoding`, `RawXmlSizeBytes`, `EmbeddedFileCount`, `EmbeddedFileNames`, `Title?`, `AssetId?`, `AssetType?`, `Description?`).
- **Sims3PackParseResult**: Immutable record containing Sims3Pack XML parsing operation status (`IsSuccess`, `Sims3PackXmlMetadata?`, `Issues`).
- **Sims3PackCatalogEntry**: Immutable record representing a scanned embedded candidate payload (`EntryIndex`, `Kind`, `DataOffset`, `EstimatedSizeBytes?`, `DisplayName?`, `Issues`).
- **Sims3PackCatalogResult**: Immutable record containing catalog scan operation status (`IsSuccess`, `Entries`, `Issues`).
- **Sims3PackPayloadExportRequest**: Value record specifying source `.sims3pack` path, `Sims3PackCatalogEntry`, target `.package` path, and `AllowOverwrite` flag.
- **Sims3PackPayloadExportResult**: Value record specifying payload export operation status (`IsSuccess`), source path, target path, exported byte count, and issues.
- **TextureResourceClassification**: Immutable record representing texture classification outcome (`PackageResourceId ResourceId`, `TextureClassificationKind Classification`, `TextureMapKind MapKind`, `string FormatName`, `GameVersion DetectedGameVersion`, `IReadOnlyList<ConversionIssue> Issues`).
- **TextureResourceExtractRequest**: Value record specifying source package path, `PackageResourceEntry`, `TextureResourceClassification`, output directory, custom filename, and `AllowOverwrite` flag.
- **TextureResourceExtractResult**: Value record specifying extraction status (`IsSuccess`), source path, output path, exported byte count, and issues.
- **DdsTextureMetadata**: Immutable record containing DDS header layout metadata (`Width`, `Height`, `MipMapCount`, `DdsTextureFormatKind FormatKind`, `FourCC`, `PixelFormatFlags`, `RgbBitCount`, `Caps`).
- **DdsParseResult**: Immutable record specifying DDS header parsing status (`IsSuccess`, `DdsTextureMetadata?`, `Issues`).
- **DdsPayloadValidationResult**: Immutable record specifying DDS payload validation status (`IsSuccess`, `ExpectedPayloadBytes`, `ActualPayloadBytes`, `Issues`).
- **MeshResourceClassification**: Immutable record representing mesh classification outcome (`PackageResourceId ResourceId`, `MeshClassificationKind Classification`, `MeshRoleKind RoleKind`, `string FormatName`, `GameVersion DetectedGameVersion`, `IReadOnlyList<ConversionIssue> Issues`).
- **MeshResourceExtractRequest**: Value record specifying source package path, `PackageResourceEntry`, `MeshResourceClassification`, output directory, custom filename, and `AllowOverwrite` flag.
- **MeshResourceExtractResult**: Value record specifying extraction status (`IsSuccess`), source path, output path, exported byte count, and issues.

### 2. SimsConverter.Textures
Contains texture resource type constants, candidate classification services, deterministic extension mapping, raw texture extraction boundaries, bounds-checked DDS binary header parsing, and overflow-safe payload size validation.

#### Key Entities & Services
- **TextureTypeIds**: Static class defining verified TS3 and TS4 texture TypeIds (`Ts3DdsTexture`, `Ts3SnapshotThumbnail`, `Ts4Rle2Texture` `0x3453CF95`, `Ts4LrleTexture` `0x2BC04EDF`, `Ts4PngImage` `0x2F7D0004`, `Ts4CasPartThumbnail` `0x3C1AF1F2`).
- **ITextureResourceClassifier**: Primary interface for texture candidate classification (`Classify`, `ClassifyBatch`).
- **TextureResourceClassifier**: Safe implementation performing deterministic TypeId classification, game version matching, map role assignment, and diagnostic issue reporting for unrecognized types (`TEXC001`).
- **ITextureResourceExtractor**: Primary interface for texture raw payload extraction (`ExtractAsync`).
- **TextureResourceExtractor**: Safe implementation verifying texture classification (`KnownTexture`), mapping extensions (.dds, .png, .rle2, .lrle), checking canonical path separation (`TEXE008`), and delegating byte range export to `IPackageResourceExporter`.
- **IDdsHeaderParser**: Primary interface for bounds-checked DDS binary header parsing (`Parse`).
- **DdsHeaderParser**: Safe implementation parsing DDS magic (`"DDS "` / `"RCOL"`), header size (128 bytes), dimensions, FourCC string, format family (`DdsTextureFormatKind`), and preserving stream invariants.
- **IDdsPayloadValidator**: Primary interface for DDS payload size estimation and truncation validation (`Validate`).
- **DdsPayloadValidator**: Safe implementation calculating exact block-compressed (8B/16B per 4x4 block) and uncompressed mipmap chain payload sizes using `checked` overflow-safe arithmetic.

### 3. SimsConverter.Mesh
Contains Phase 3 mesh/geometry resource type constants, candidate classification foundation, deterministic extension mapping, raw payload extraction boundaries, TS3 GEOM structural metadata reader, and TS3 GEOM CanonicalMesh importer.

#### Key Entities & Services
- **MeshTypeIds**: Static class defining verified TS3, TS4, and shared mesh/geometry TypeIds (`Ts3Geom` `0x015A1849`, `TsSharedModel` `0x01661233`, `TsSharedModelLod` `0x01D10F34`, `TsSharedRig` `0x8EAF13DE`, `TsSharedSlot` `0xD3044521`, `TsSharedBlendGeometry` `0x067CAA11`).
- **IMeshResourceClassifier**: Primary interface for mesh candidate classification (`Classify`, `ClassifyBatch`).
- **MeshResourceClassifier**: Safe implementation performing deterministic TypeId classification, mesh role assignment (`Geometry`, `Rig`, `Morph`, `Slot`), game version matching, and diagnostic issue reporting for unrecognized types (`MESHC001`).
- **IMeshResourceExtractor**: Primary interface for mesh raw payload extraction (`ExtractAsync`).
- **MeshResourceExtractor**: Safe implementation verifying mesh classification (`KnownMesh`), mapping extensions (.geom, .modl, .mlod, .rig, .rslt, .bgeo), checking ResourceId identity match (`MESHE004`), sanitizing custom file names, enforcing canonical output path boundary (`MESHE008`), and delegating byte range export to `IPackageResourceExporter`.
- **ITs3GeomMetadataReader**: Primary interface for TS3 GEOM header and structural metadata reading (`Read`, `ReadAsync`).
- **Ts3GeomMetadataReader**: Safe Little-Endian implementation aligned with `blender-sims3-geom` reference specifications (`geom_write.py`), evaluating candidate RCOL layouts (Count-First, Tagged `"RCOL"`, Numeric Version) via `EvaluateLayoutCandidate`, parsing internal ITG 16-byte TGIs (`InstanceId` 8B, `TypeId` 4B, `GroupId` 4B), external resource 16-byte TGIs, 8-byte Chunk Location Tables (`Position` 4B, `Size` 4B), strict chunk slice boundary validation (`GEOM004`), internal GEOM chunk offset discovery (`"GEOM"` magic signature), GEOM chunk versioning (`GEOM002`), exact `tgiOffset` formula (`actualTgiPos = 12 + rawTgiOffset`, `geom_write.py` lines 62 & 143), embedded TGI section size formula verification (`tgiSize == 4 + tgiCount * 16`, `GEOM005`), optional `"MTNF"` shader block skipping with `checked` overflow arithmetic (`GEOM004`/`GEOM005`), 4-byte zero padding word skip when `EmbeddedId == 0`, 9-byte vertex element descriptors (`DataType` 4B, `Format` 4B, `SizeBytes` 1B), face group section (`groupMarker` 4B + `faceFormat` 1B + `facePointCount` 4B, `FacePointCount % 3 == 0` check, `GEOM003`), index buffer skips, skin controllers, bone hash tail truncation guards (`curr + boneCount * 4 <= chunkSpan.Length`), and payload truncation bounds (`GEOM005`).
- **ITs3GeomCanonicalMeshImporter**: Primary interface for TS3 GEOM binary vertex and index buffer decoding into game-agnostic canonical mesh models (`Import`, `ImportAsync`).
- **Ts3GeomCanonicalMeshImporter**: Safe Little-Endian implementation executing bounds-checked binary decoding of 9-byte vertex element descriptors (Position, Normal, UV0, UV1, Bone Indices, Bone Weights, Tangent), vertex float buffers, UInt16 index buffers into `CanonicalFace` triangle lists, instantiating `CanonicalMesh`, and validating outcomes via `ICanonicalMeshValidator`. Operates strictly on `buffer.Slice(metadata.GeomChunkOffset, metadata.GeomChunkSize)`.

### 4. SimsConverter.Application
Contains application use-case orchestration services, contracts, and UI-independent presentation models.

#### Key Entities & Services
- **IPackageInspectionService**: Application use-case contract for package resource inspection (`InspectFileAsync`).
- **PackageInspectionService**: Use-case orchestration service translating binary `DbpfParseResult` into presentation-ready `PackageInspectionResult`.
- **IResourceExportService**: Application contract for single raw resource export (`ExportResourceAsync`).
- **ResourceExportService**: Use-case service generating sanitized deterministic output filenames (`00B2D882_00000000_123456789ABCDEF0.raw`) and delegating export to Package layer.
- **SingleResourceExportRequest**: Application request model combining source path, selected `PackageResourceRow`, destination directory, and overwrite flag.
- **PackageInspectionResult**: Presentation result record containing status (`IsSuccess`), file path, header summary, resource rows, and issues.
- **PackageResourceRow**: Presentation-ready row record exposing hex-formatted keys (`TypeHex`, `GroupHex`, `InstanceHex`, `FormattedKey`), offsets, sizes, and compression names.
- **ISims3PackInspectionService**: Application use-case contract for Sims3Pack inspection and embedded payload export (`InspectFileAsync`, `ExportPayloadAsync`).
- **Sims3PackInspectionService**: Orchestration service unifying detection, XML parsing, catalog scanning, and payload export into UI-ready presentation results.
- **Sims3PackPayloadRow**: Presentation-ready row model (`EntryIndex`, `Kind`, `DataOffsetHex`, `EstimatedSizeFormatted`, `DisplayName`, `CanExport`, `Issues`).
- **Sims3PackInspectionResult**: Presentation result record containing status (`IsSuccess`), file path, XML metadata fields, `PayloadRows`, and aggregated `Issues`.
- **Sims3PackExportRequest**: Application export request model (`SourceSims3PackPath`, `SelectedRow`, `OutputDirectory`, `CustomFileName`, `AllowOverwrite`).
- **Sims3PackExportResult**: Application export result record containing status (`IsSuccess`), file paths, exported byte count, and `Issues`.
- **ITextureInspectionService**: Application use-case contract for texture resource inspection and UI row projection (`InspectPackageTexturesAsync`, `InspectPackageTextures`).
- **TextureInspectionService**: Application use-case service translating package resources into presentation-ready `TextureResourceRow` models using `ITextureResourceClassifier`.
- **TextureInspectionRequest**: Application request model (`PackageFilePath`, `GameVersionHint`).
- **TextureResourceRow**: Presentation-ready row record exposing formatted hex keys, format names, map kinds, game versions, capability flags (`CanExtractRawPayload`, `CanParseDdsHeader`), and issues.
- **TextureInspectionResult**: Presentation result record containing status (`IsSuccess`), file path, `Rows`, and aggregated `Issues`.
- **IMeshInspectionService**: Application use-case contract for mesh resource inspection and UI row projection (`InspectPackageMeshesAsync`, `InspectPackageMeshes`).
- **MeshInspectionService**: Application use-case service orchestrating package resource entries, mesh classification (`IMeshResourceClassifier`), and TS3 GEOM canonical mesh decoding (`ITs3GeomCanonicalMeshImporter`) into presentation-ready `MeshResourceRow` models.
- **MeshResourceRow**: Presentation-ready row record exposing formatted hex keys, classification kinds, roles, game versions, format names, capabilities (`CanExtractRawPayload`, `CanInspectCanonicalMesh`), and canonical mesh decoding summary fields (`VertexCount`, `FaceCount`, `BoneCount`, `HasNormals`, `HasUv0`, `HasBoneWeights`, `ValidationIssueCount`).
- **MeshInspectionResult**: Presentation result record containing status (`IsSuccess`), file path, `Rows`, and aggregated `Issues`.
- **IDecorativeObjectConversionService**: Application use-case contract for TS3 to TS4 decorative object conversion planning (`CreateConversionPlanAsync`, `CreateConversionPlan`).
- **DecorativeObjectConversionService**: Application service orchestrating source package inspection, mesh/texture candidate collection, TS3 GEOM canonical import verification, and target writer capability evaluation into `DecorativeObjectConversionPlan`.
- **DecorativeObjectConversionRequest**: Application conversion request model (`SourcePackagePath`, `TargetOutputPath`, `TargetGameVersion`, `ObjectName`).
- **DecorativeObjectConversionResult**: Application result record (`IsSuccess`, `SourcePackagePath`, `TargetOutputPath`, `Plan`, `Issues`).
- **DecorativeObjectConversionPlan**: Conversion plan record containing target game version, candidate counts (`TotalMeshCandidateCount`, `TotalTextureCandidateCount`, `ConvertableMeshCount`, `ValidTextureCount`), candidate rows (`MeshCandidates`, `TextureCandidates`), execution steps (`Steps`), feasibility status (`IsFeasible`), and optional `SourceGraph`.
- **DecorativeObjectConversionStep**: Individual conversion plan step record (`StepId`, `Title`, `Status`, `Details`, `Issues`).
- **IDecorativeObjectSourceGraphBuilder**: Application use-case contract for building TS3 decorative object source asset graphs (`BuildGraphAsync`, `BuildGraph`).
- **DecorativeObjectSourceGraphBuilder**: Application service constructing `DecorativeObjectSourceAssetGraph` from package inspection, classifying mesh/texture assets, tracking resource links, and calculating source graph readiness.
- **DecorativeObjectSourceAssetGraph**: Graph model containing `SourcePackagePath`, `TotalResourceCount`, `MeshAssets`, `TextureAssets`, `ResourceLinks`, `OtherResources`, `HasImportableMesh`, `HasTextureCandidates`, `IsSourceGraphReady`, and `Issues`.
- **DecorativeObjectSourceMeshAsset**: Graph mesh node record (`FormattedKey`, `ResourceId`, `ClassificationKind`, `RoleKind`, `FormatName`, `DetectedGameVersion`, `CanInspectCanonicalMesh`, `VertexCount`, `FaceCount`, `BoneCount`, `Issues`).
- **DecorativeObjectSourceTextureAsset**: Graph texture node record (`FormattedKey`, `ResourceId`, `ClassificationKind`, `MapKind`, `FormatName`, `CanExtractRawPayload`, `Issues`).
- **DecorativeObjectSourceResourceLink**: Graph edge link record (`SourceKey`, `TargetKey`, `RelationshipType`).

### 5. SimsConverter.Package
Contains contracts and data structures for DBPF, package container inspection, Sims3Pack container detection, XML manifest parsing, catalog scanning, safe raw binary byte range extraction, and real fixture validation.

#### Key Entities & Contracts
- **PackageHeaderSummary**: Record summarizing index count, DBPF versioning, and detected game version.
- **IPackageInspector**: Interface for package header inspection.
- **IPackageDetector**: Primary interface for safe, bounds-checked container inspection (`Detect`, `DetectAsync`, `DetectFileAsync`).
- **PackageDetector**: Safe implementation executing non-destructive DBPF container detection and delegating `.sims3pack` files to `ISims3PackDetector`.
- **ISims3PackDetector**: Dedicated contract for `.sims3pack` binary header detection (`DetectFileAsync`, `DetectAsync`).
- **Sims3PackDetector**: Implementation executing binary TS3Pack Little-Endian header detection (`signatureLength`, `"TS3Pack"`, `version`, `xmlLength`).
- **ISims3PackXmlParser**: Primary contract for parsing Sims3Pack XML metadata sections safely (`ParseFileAsync`, `ParseAsync`).
- **Sims3PackXmlParser**: Implementation executing exact `xmlLength` byte reads and secure XML parsing (XXE/DTD disabled, 10MB allocation limit).
- **ISims3PackPayloadCatalogScanner**: Primary contract for scanning Sims3Pack archive payloads (`ScanFileAsync`, `ScanAsync`).
- **Sims3PackPayloadCatalogScanner**: Non-destructive implementation scanning archive sections for DBPF (`"DBPF"`) and PNG magic signatures, ordering entries by `DataOffset`.
- **ISims3PackPayloadExporter**: Primary contract for exporting embedded DBPF payload candidates from Sims3Pack containers (`ExportAsync`).
- **Sims3PackPayloadExporter**: Safe implementation executing bounds-checked DBPF byte range extraction, canonical path check (`S3PE008`), DBPF magic verification (`S3PE007`), and atomic temp file creation.
- **Sims3PackRealFixtureValidationTests**: Test harness for validating real-world `.sims3pack` files in `fixtures/local/` and generating compatibility reports without committing binary game files.
- **IDbpfPackageParser**: Primary contract for parsing DBPF container headers and resource index entry tables safely (`Parse`, `ParseAsync`, `ParseFileAsync`).
- **DbpfPackageParser**: Robust parser implementation performing bounds checking, Little-Endian reading, and safe index extraction.
- **IPackageResourceExporter**: Primary contract for raw binary byte range export (`ExportAsync`).
- **PackageResourceExporter**: Implementation executing read-only byte range extraction, bounds checking, overwrite protection, canonical path check (`EXPE008`), and atomic temp file creation.

### 6. SimsConverter.App
Avalonia UI application root containing composition root, dependency injection, and MVVM presentation ViewModels.

#### Key Components
- **App Composition Root**: Wires `IDbpfPackageParser`, `IPackageInspectionService`, `IPackageResourceExporter`, `IResourceExportService`, `ISims3PackDetector`, `ISims3PackXmlParser`, `ISims3PackPayloadCatalogScanner`, `ISims3PackPayloadExporter`, `ISims3PackInspectionService`, `IPackageDetector`, `IFilePickerService`, `ITextureResourceClassifier`, `ITextureResourceExtractor`, `IDdsHeaderParser`, `IDdsPayloadValidator`, `ITextureInspectionService`, `IMeshResourceClassifier`, `IMeshResourceExtractor`, `ICanonicalMeshValidator`, `ITs3GeomMetadataReader`, `ITs3GeomCanonicalMeshImporter`, `IMeshInspectionService`, and `ResourceInspectorViewModel` via `Microsoft.Extensions.DependencyInjection`.
- **ResourceInspectorViewModel**: Reactive ViewModel (`CommunityToolkit.Mvvm`) managing `SelectedFilePath`, `SelectedResource`, `SelectedSims3PackPayload`, `SelectedTextureResource`, `IsSims3PackMode`, `IsBusy`, `StatusMessage`, `Resources`, `Sims3PackPayloads`, `TextureResources`, `Issues`, `CanInspect`, `CanExport`, `CanExportSims3PackPayload`, `CanExtractSelectedTexture`, `CanParseSelectedDdsHeader`, `InspectCommand`, `ExportResourceCommand`, and `ExportSims3PackPayloadCommand`.
- **MainWindow.axaml**: Semantic XAML view bound to `ResourceInspectorViewModel` with monospaced hex DataGrid columns, Sims3Pack metadata header, TabControl containing Package Resources and Texture Candidates DataGrids, payload DataGrid, issue diagnostic border, and action buttons.

---

## Strategic Design Rules
1. `SimsConverter.Domain` MUST NOT depend on any external libraries or other projects in the solution.
2. Domain entities must remain immutable or encapsulate state changes strictly.
3. `SimsConverter.Application` MUST NOT reference Avalonia UI or presentation assemblies (`SimsConverter.App`), and MUST NOT perform low-level binary byte parsing.
4. `SimsConverter.App` MUST NOT perform binary parsing or directly reference `System.Buffers.Binary.BinaryPrimitives`, `System.Xml`, or low-level file streams. All inspection, XML parsing, catalog scanning, and export operations MUST execute via application/package services.
5. **Prohibition of Magic-Only Guessing**: Detecting DBPF container magic alone MUST NOT mark the game version as `Sims3` or `Sims4`. Game version MUST remain `GameVersion.Unknown` until verified by game-specific resource signatures.
6. **Sims3Pack Container Policy**: Sims3Pack XML manifest parsing MUST enforce secure settings (`DtdProcessing = Prohibit`, `XmlResolver = null`) to prevent XXE and DTD expansion attacks, and MUST read exactly `xmlLength` bytes starting from calculated `headerLength`.
7. **Sims3Pack Archive Catalog Policy**: Archive scanning MUST be read-only (`FileShare.Read`) and non-destructive. Payload candidates MUST be detected by magic signature (`"DBPF"`, PNG magic) and returned in deterministic order by `DataOffset`.
8. **Sims3Pack Embedded Payload Export Policy**: Embedded DBPF payload export MUST verify candidate kind (`DbpfPackage`), canonical path separation (`S3PE008`), post-export DBPF magic header verification (`S3PE007`), and atomic temp file creation.
9. **Sims3Pack Application Boundary Policy**: Application inspection services MUST aggregate diagnostic issues across detection, XML parsing, and catalog scanning, and MUST project domain catalog entries into presentation-ready `Sims3PackPayloadRow` models for UI consumption.
10. **Real Fixture Validation Policy**: Real-world `.sims3pack` files MUST be stored in `fixtures/local/` and ignored by `.gitignore`. Validation tests MUST execute gracefully in a skipped state when no local fixtures are present.
11. **Texture Classification Policy**: Texture candidate classification MUST be deterministic based on verified TypeId constants (`0x3453CF95` [RLE2], `0x2BC04EDF` [LRLE], `0x2F7D0004` [PNG Image], `0x3C1AF1F2` [CAS Part Thumbnail]). Unverified TypeIds MUST NOT be added to the catalog. Unambiguous engine-specific TypeIds map automatically to `GameVersion.Sims4`; ambiguous TypeIds (`0x00B2D882`) retain `GameVersion.Unknown` unless an explicit game version hint is provided. Unrecognized TypeIds MUST produce diagnostic warning issues (`TEXC001`).
12. **Texture Payload Extraction Policy**: Texture payload extraction MUST verify that `Classification == KnownTexture` before exporting (`TEXE001`). `Classification.ResourceId` MUST match `Entry.Id` (`TEXE004`). Raw byte range copying MUST delegate to `IPackageResourceExporter`. Extensions MUST map deterministically (.dds, .png, .rle2, .lrle). Canonical path separation MUST be enforced (`TEXE008`).
13. **DDS Header Parsing Policy**: DDS binary header parsing MUST enforce minimum 128-byte size (`TEXD001`), `"DDS "` magic signature (`TEXD002`), positive dimensions (`TEXD003`), struct sizes (`TEXD005`, `TEXD006`), format family classification (`DXT1`, `DXT3`, `DXT5`, `ATI1`, `ATI2`, `UncompressedRgba`), and stream position invariants. Unrecognized FourCC values MUST produce diagnostic warning issues (`TEXD004`). Zero pixel decoding or image rendering is permitted in the header parser.
14. **DDS Payload Validation Policy**: DDS payload size estimation MUST calculate full mipmap chain level byte counts using exact block formulas ($4 \times 4$ blocks, 8B for DXT1/ATI1, 16B for DXT3/DXT5/ATI2) and pitch calculations for uncompressed RGBA. MipMapCount MUST NOT exceed the maximum valid mip count ($\lfloor \log_2(\max(W, H)) \rfloor + 1$), returning controlled error `TEXV005` if exceeded. Arithmetic calculations MUST use `checked` blocks to detect overflow (`TEXV003`). Truncated payloads MUST fail validation with error `TEXV001`. Unknown formats MUST return `IsSuccess = false` with diagnostic warning `TEXV004`.
15. **Texture Inspection Application Boundary Policy**: `TextureInspectionService` MUST project package entries into presentation-ready `TextureResourceRow` models using `ITextureResourceClassifier`. Capability flags (`CanExtractRawPayload`, `CanParseDdsHeader`) MUST be set deterministically based on classification outcome. Unknown resources MUST be included with `ClassificationKind = Unknown` and diagnostic warning `TEXC001` (NEVER silently omitted). Zero low-level binary byte parsing or file stream reads are permitted in the Application layer.
16. **Mesh Classification Policy**: Mesh candidate classification MUST be deterministic based on verified TypeId constants (`0x015A1849` [TS3 GEOM], `0x01661233` [Shared MODL], `0x01D10F34` [Shared MLOD], `0x8EAF13DE` [Shared RIG], `0xD3044521` [Shared RSLT Slot], `0x067CAA11` [Shared BGEO Morph]). Unverified TypeIds MUST NOT be added to the catalog. Shared TypeIds (`0x01661233`, `0x01D10F34`, `0x8EAF13DE`, `0xD3044521`, `0x067CAA11`) MUST remain `GameVersion.Unknown` unless an explicit game version hint is provided. Unrecognized TypeIds MUST produce diagnostic warning issues (`MESHC001`).
17. **Mesh Payload Extraction Policy**: Mesh payload extraction MUST verify that `Classification == KnownMesh` before exporting (`MESHE001`). `Classification.ResourceId` MUST match `Entry.Id` (`MESHE004`). Raw byte range copying MUST delegate to `IPackageResourceExporter`. Custom file names MUST be sanitized using `InvalidFileNameCharRegex` to remove control characters and path separators. Canonical output path MUST be verified to prevent directory traversal (`MESHE008`). Target output path MUST not be identical to source package file path (`MESHE008`). Extensions MUST map deterministically (.geom, .modl, .mlod, .rig, .rslt, .bgeo). Zero binary mesh structure parsing or 3D buffer decoding is permitted in the Mesh layer.
18. **Canonical Mesh Validation & Init-Setter Immutability Policy**: Canonical mesh validation MUST verify non-null mesh (`MESHV000`), non-empty vertex list (`MESHV001`), non-empty face list (`MESHV002`), face index bounds ($0 \le \text{idx} < \text{vertexCount}$, `MESHV003`), valid vertex position coordinates (no `NaN` or `Infinity`, `MESHV005`), degenerate triangle detection (`A == B`, `B == C`, `A == C`, `MESHV004` warning), and bone weight sum normalization ($[0.99, 1.01]$, `MESHV006` warning). `CanonicalMesh` and `CanonicalVertex` properties MUST be get-only (`{ get; }`) and perform defensive copy snapshot initialization (`Array.AsReadOnly(items.ToArray())`), preventing object initializer or `with` expression init-setter bypasses. Domain models MUST remain game-agnostic and free of external 3D engine dependencies.
19. **TS3 GEOM Reference-Layout RCOL Structural Metadata Reader Policy**: TS3 GEOM metadata reading MUST follow the `blender-sims3-geom` reference layout (`geom_write.py`), evaluating candidate RCOL layouts (Count-First, Tagged `"RCOL"`, Numeric Version) via `EvaluateLayoutCandidate`, parsing internal ITG 16-byte TGIs (`InstanceId` 8B, `TypeId` 4B, `GroupId` 4B), external resource 16-byte TGIs, 8-byte Chunk Location Tables (`Position` 4B, `Size` 4B), strict chunk slice boundary validation (`GEOM004`), internal GEOM chunk offset discovery (`"GEOM"` magic signature), GEOM chunk versioning (`GEOM002`), exact `tgiOffset` formula (`actualTgiPos = 12 + rawTgiOffset`, `geom_write.py` lines 62 & 143), embedded TGI section size formula verification (`tgiSize == 4 + tgiCount * 16`, `GEOM005`), optional `"MTNF"` shader block skipping with `checked` overflow arithmetic (`GEOM004`/`GEOM005`), 4-byte zero padding word skip when `EmbeddedId == 0`, 9-byte vertex element descriptors (`DataType` 4B, `Format` 4B, `SizeBytes` 1B), face group section (`groupMarker` 4B + `faceFormat` 1B + `facePointCount` 4B, `FacePointCount % 3 == 0` check, `GEOM003`), index buffer skips, skin controllers, bone hash tail truncation guards (`curr + boneCount * 4 <= chunkSpan.Length`), and payload truncation bounds (`GEOM005`).
22. **Mesh Inspection Application Boundary Policy**: `MeshInspectionService` MUST project package entries into presentation-ready `MeshResourceRow` models using `IMeshResourceClassifier`, `ITs3GeomCanonicalMeshImporter`, and `ITs4GeomCanonicalMeshImporter`. Raw resource payload byte reading MUST delegate to `IPackageResourcePayloadReader` in `SimsConverter.Package`. Known GEOM entries (`0x015A1849`) MUST delegate to `ITs4GeomCanonicalMeshImporter` when `DetectedGameVersion == GameVersion.Sims4` and to `ITs3GeomCanonicalMeshImporter` when `DetectedGameVersion == GameVersion.Sims3` (or default). Capability flags (`CanExtractRawPayload`, `CanInspectCanonicalMesh`) MUST be set deterministically. Unknown resources MUST be included with `ClassificationKind = Unknown` and diagnostic warning `MESHC001` (NEVER silently omitted). Importer failures MUST NOT crash the application or omit rows, setting `CanInspectCanonicalMesh = false` and attaching diagnostic issues to the row. Zero low-level binary byte parsing, `File.ReadAllBytes`, or `AsSpan`/`Slice` calls are permitted in the Application layer.
23. **Mesh Inspection UI Integration Policy**: Package resource inspection UI MUST present mesh candidates in a dedicated `TabControl` tab ("Mesh Candidates"), exposing formatted hex keys (`CellStyleClasses="monospaced"`), format names, mesh roles, game versions, capability flags (`CanExtractRawPayload`, `CanInspectCanonicalMesh`), and canonical mesh decoding summary fields (`VertexCount`, `FaceCount`, `BoneCount`, `HasNormals`, `HasUv0`, `HasBoneWeights`, `Issues.Count`) without performing binary GEOM decoding or stream slicing in the UI assembly.
24. **TS4 Mesh Resource Format Research & Type Boundary Policy**: Every TS4 mesh production constant in `MeshTypeIds.cs` MUST be verified against authoritative primary/secondary documentation sources (`0x015A1849` [GEOM / TsSharedGeom], `0x01661233` [MODL], `0x01D10F34` [MLOD], `0x8EAF13DE` [RIG], `0xD3044521` [RSLT], `0x067CAA11` [BGEO]). Unverified candidate TypeIds (e.g. `0x025C6425`, `0x02864C99`) MUST NOT be registered as production constants without sample package verification and MUST fall back cleanly to `Classification = Unknown` with warning issue `MESHC001`. `MeshResourceClassifier` MUST handle game version hints (`GameVersion.Sims4`) to format resource names correctly without breaking TS3 GEOM classification regressions.
25. **TS4 CAS GEOM Structural Metadata Reader Policy**: TS4 CAS GEOM metadata reading MUST support raw GEOM buffers starting with `"GEOM"` magic at offset 0 and RCOL-wrapped GEOM container buffers, validate `"GEOM"` magic signature (`GEOM001`), validate GEOM version range ($5, 12, 13, 14$) (`GEOM002`), parse structural count fields (`VertexCount`, `FaceCount`, `FacePointCount`, `VertexStrideBytes`, `VertexElementCount`, `BoneCount`) using `checked` arithmetic overflow protection (`GEOM004`), verify `FacePointCount % 3 == 0` (`GEOM003`), enforce truncation guards (`GEOM000`, `GEOM005`), and MUST NEVER mutate source payload buffers during reading. Zero vertex/index buffer decoding, `CanonicalMesh` generation, or writer operations are permitted in the metadata reader.
26. **TS4 CAS GEOM CanonicalMesh Importer Policy**: TS4 CAS GEOM mesh importing MUST perform bounds-checked binary decoding of 9-byte vertex element descriptors (Position, Normal, UV0, Bone Indices, Bone Weights, Tangent, Vertex ID), vertex float buffers, index buffers (`bytesPerFacePoint` values `1`, `2`, `4`) into `CanonicalFace` triangle lists, instantiate `CanonicalMesh` (`CoordinateSystem.RightHandedYUp`, `SourceGameVersion.Sims4`), and execute `ICanonicalMeshValidator.Validate(canonicalMesh)` before returning `Ts4GeomImportResult`. Bone weight descriptors (`datatype == 5`) MUST validate `format == 1 && sizeBytes >= 16` for Float4 or `format == 2 && sizeBytes >= 4` for Byte4 normalized, returning diagnostic error `MESHG008` for unsupported formats or insufficient `sizeBytes`. Unrecognized vertex datatypes MUST produce diagnostic warning issues (`MESHG003`). Missing Position descriptor MUST produce diagnostic error issue (`MESHG004`). Unsupported `bytesPerFacePoint` values MUST produce diagnostic error issue (`MESHG007`). Truncated vertex/index buffers MUST return controlled error results (`MESHG002`). Source payload buffers MUST NOT be modified during import. Operates strictly on `buffer.Slice(metadata.GeomChunkOffset, metadata.GeomChunkSize)`.
27. **Phase 3 Mesh Pipeline Real Fixture Validation & Compatibility Policy**: Real-world binary sample `.package` files MUST be stored under `fixtures/local/mesh/` or `fixtures/local/` and ignored by `.gitignore`, preventing git commits of proprietary binary content. Automated real-fixture test harnesses (`MeshInspectionRealFixtureValidationTests`) MUST scan exclusively for `*.package` files and execute clean no-op skips when local sample `.package` files are absent, avoiding CI/CD build failures. When local `.package` fixtures are present, end-to-end inspection MUST validate `PackageInspectionService` $\rightarrow$ `MeshResourceClassifier` $\rightarrow$ `IPackageResourcePayloadReader` $\rightarrow$ `ITs3GeomCanonicalMeshImporter` / `ITs4GeomCanonicalMeshImporter` $\rightarrow$ `MeshInspectionService` pipeline execution without throwing unhandled exceptions. Raw `.geom` files and `.sims3pack` containers are tested via dedicated unit test suites (`Ts3GeomMetadataReaderTests`, `Ts4GeomMetadataReaderTests`, `Sims3PackInspectionServiceTests`). Importer output compatibility MUST be documented in `docs/reports/mesh_compatibility_report.md`. Zero writer, export, or TS4 package creation operations are permitted in Phase 3.
28. **Decorative Object Conversion Boundary Policy**: `DecorativeObjectConversionService` MUST evaluate conversion requests using `IPackageInspectionService`, `IMeshInspectionService`, and `ITextureInspectionService`. Source package path MUST exist (`CONVA001`). Target output path MUST NOT be empty (`CONVA002`) and MUST NOT be identical to source package path (`CONVA003`), enforcing path canonicalization via `Path.GetFullPath`. Target file creation or writing is STRICTLY FORBIDDEN during conversion plan construction. Step 7 (`STEP-07-TS4-WRITER`) MUST deterministically return status `NotImplemented`. Zero low-level binary byte parsing or stream manipulation is permitted in the Application layer.
29. **TS3 Decorative Object Source Asset Graph Policy**: `DecorativeObjectSourceGraphBuilder` MUST analyze source packages using `IMeshInspectionService` and `ITextureInspectionService`, constructing a graph of mesh assets (`DecorativeObjectSourceMeshAsset`), texture assets (`DecorativeObjectSourceTextureAsset`), and resource links (`DecorativeObjectSourceResourceLink`). `ResourceLinks` MUST NOT create speculative, unverified mesh-to-texture links. `HasImportableMesh` MUST evaluate to `true` ONLY when a candidate satisfies: `ClassificationKind == KnownMesh`, `DetectedGameVersion == Sims3`, `RoleKind == Geometry`, `TypeId == 0x015A1849` (TS3 GEOM), and `CanInspectCanonicalMesh == true`. If a package contains object model resources (MODL `0x01661233` or MLOD `0x01D10F34`) but no direct TS3 GEOM, `HasImportableMesh` MUST be `false` and controlled warning `CONVG003` attached (`"No direct TS3 GEOM resource found; object model conversion requires MODL/MLOD decomposition."`). If no importable mesh is present, `IsSourceGraphReady` MUST be `false`, setting `STEP-06-SOURCE-GRAPH` to `Failed` and `Plan.IsFeasible` to `false`. Unrecognized or non-TS3 GEOM resources (MODL, MLOD, RIG, RSLT, COBJ, CBLK, etc.) MUST be retained in `OtherResources` (NEVER silently dropped). Zero file creation or binary parsing is permitted in the Application layer.
30. **DBPF Index Sanity & Candidate Count Accuracy Policy**: `DbpfPackageParser` MUST reject `rawIndexOffset == 0` when `indexEntryCount == 0` or index offsets pointing inside the DBPF header (`0 < rawIndexOffset < minHeaderSize`, error `PARSE005`). Resource entry payload bounds (`DataOffset + CompressedSize <= fileLength`) MUST be verified (`PARSE015`). Entries with `TypeId` matching DBPF header magic (`0x46504244` / `0x44425046`) MUST be flagged with error `PARSE016`. `ResourceInspectorViewModel` status bar candidate counts MUST count ONLY `KnownTexture` and `KnownMesh` entries. `Unknown` classification rows remain visible in DataGrids for diagnostic inspection, but MUST NOT be counted as candidates in the UI status summary.
31. **Sims3Pack Embedded DBPF Payload Validation & Export Hardening Policy**: `Sims3PackPayloadCatalogScanner` MUST peek and inspect the 96-byte DBPF header (`ValidateDbpfHeader`) at every candidate offset, classifying candidates with corrupt header parameters (`rawIndexOffset == 0` with `indexEntryCount == 0`, `0 < rawIndexOffset < minHeaderSize`, etc.) as `Sims3PackPayloadKind.InvalidDbpfPackage` with diagnostic issue `S3PC010`. `Sims3PackInspectionService` MUST set `CanExport = false` for non-`DbpfPackage` candidates. `Sims3PackPayloadExporter` MUST reject export requests for non-`DbpfPackage` candidates (`S3PE001`) and run post-export DBPF header sanity validation (`ValidateDbpfHeader`) before atomic file move (`S3PE007`).
32. **TS3 Embedded DBPF Implicit Layout Compatibility Policy**: `DbpfPackageParser` and `Sims3PackPayloadCatalogScanner.ValidateDbpfHeader` MUST support TS3 embedded package implicit layout resolution. When `rawIndexOffset == 0` AND `indexEntryCount > 0`, `effectiveIndexOffset` MUST be resolved to `96` (`MinimumHeaderBufferSize`), proceeding with index entry extraction. If `rawIndexOffset == 0` AND `indexEntryCount == 0`, the header MUST be rejected (`PARSE005` in parser, `S3PC010` in scanner). If `rawIndexOffset > 0` AND `rawIndexOffset < minHeaderSize`, the header MUST be rejected as invalid (`PARSE005`).
24. **Mesh Inspection Application Boundary Policy**: `MeshInspectionService` MUST project package entries into presentation-ready `MeshResourceRow` models using `IMeshResourceClassifier` and `ITs3GeomCanonicalMeshImporter`. Capability flags (`CanExtractRawPayload`, `CanInspectCanonicalMesh`) and canonical mesh decoding summaries (`VertexCount`, `FaceCount`, `BoneCount`, `HasNormals`, `HasUv0`, `HasBoneWeights`, `ValidationIssueCount`) MUST be populated deterministically. Unknown mesh resources MUST be included with `ClassificationKind = Unknown` and diagnostic warning `MESHC001` (NEVER silently omitted). Zero low-level binary byte parsing or stream slicing is permitted in the Application layer.
